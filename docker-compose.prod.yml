# Override pour production (VPS Hetzner).
# Usage : docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
#
# Différences vs dev :
# - Frontend en build optimisé (Next.js standalone)
# - Pas de volume mount du code (image figée)
# - Pas de --reload sur uvicorn
# - Postgres self-hosted (free, illimité ; auparavant Prisma cloud bloqué par plan)

services:
  # redis : plus d'override nécessaire. Le port n'est plus exposé dans le fichier
  # base (Docker Compose CONCATÈNE les listes `ports` entre fichiers, donc un
  # `ports: []` ici n'effaçait PAS le mapping du base — c'était le bug à l'origine
  # du report BSI/CERT-Bund du 19/05/2026). Mot de passe + pas de port = sécurisé.

  postgres:
    # Expose seulement sur loopback (127.0.0.1) pour permettre `npx prisma db push`
    # depuis l'host. Pas accessible depuis l'extérieur (firewall + loopback only).
    ports:
      - "127.0.0.1:5432:5432"

  backend:
    restart: unless-stopped
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
    volumes: []
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      ENVIRONMENT: production
      SECRET_KEY: ${SECRET_KEY}
      ODDS_API_KEY: ${ODDS_API_KEY}
      FOOTBALL_DATA_API_KEY: ${FOOTBALL_DATA_API_KEY}
      SENTRY_DSN: ${SENTRY_DSN:-}
      CORS_ORIGINS: ${CORS_ORIGINS:-}
      # Chatbot Anthropic Claude Haiku (glossaire edgeAI)
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      # Instagram publication automatique
      INSTAGRAM_ACCESS_TOKEN: ${INSTAGRAM_ACCESS_TOKEN:-}
      INSTAGRAM_ACCOUNT_ID: ${INSTAGRAM_ACCOUNT_ID:-}
      API_BASE_URL: ${API_BASE_URL:-https://edgeai-betting.duckdns.org}
      INSTAGRAM_SERVICE_TOKEN: ${INSTAGRAM_SERVICE_TOKEN:-}

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: production
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/}
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}
    restart: unless-stopped
    volumes: []
    command: ["npm", "start"]
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/}
      NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}
      SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}

  ml_worker:
    restart: unless-stopped
    volumes:
      - ./ml/artifacts:/app/artifacts
      - ./ml/data:/app/data
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      FOOTBALL_DATA_API_KEY: ${FOOTBALL_DATA_API_KEY}
      ODDS_API_KEY: ${ODDS_API_KEY}
      MODEL_DIR: /app/artifacts/models
      PER_LEAGUE_MODEL_LEAGUES: ${PER_LEAGUE_MODEL_LEAGUES:-Serie A}
      # Notifications Brevo
      BREVO_API_KEY: ${BREVO_API_KEY:-}
      NOTIFICATION_EMAIL_TO: ${NOTIFICATION_EMAIL_TO:-}
      NOTIFICATION_EMAIL_FROM: ${NOTIFICATION_EMAIL_FROM:-}
      APP_BASE_URL: ${APP_BASE_URL:-https://edgeai-betting.duckdns.org}
      INSTAGRAM_SERVICE_TOKEN: ${INSTAGRAM_SERVICE_TOKEN:-}
      VALUE_BET_LEAGUES: ${VALUE_BET_LEAGUES:-Ligue 1,Bundesliga,Serie A,World Cup}
      VALUE_BET_OU_LEAGUES: ${VALUE_BET_OU_LEAGUES:-Premier League}
      VALUE_BET_AH_LEAGUES: ${VALUE_BET_AH_LEAGUES:-Ligue 1,Premier League,Serie A,World Cup}
